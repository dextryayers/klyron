[package]
name = "klyron-engine-quickjs"
version = "1.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
klyron-engine-common = { path = "../common" }

[build-dependencies]
cc = "1"
