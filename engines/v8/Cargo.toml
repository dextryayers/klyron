[package]
name = "klyron-engine-v8"
version = "1.1.0"
edition = "2024"

[features]
default = []
native = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
libc = "0.2"
klyron-engine-common = { path = "../common" }

[build-dependencies]
cc = "1"
pkg-config = "0.3"
