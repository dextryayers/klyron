# Klyron workspace Cargo configuration
[alias]
test-all = "test --workspace --no-fail-fast"
check-all = "check --workspace"
build-all = "build --workspace"
fix-all = "fix --workspace"

[profile.release]
lto = "thin"
codegen-units = 1
