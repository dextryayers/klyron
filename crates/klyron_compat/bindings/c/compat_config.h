#ifndef KLYRON_COMPAT_CONFIG_H
#define KLYRON_COMPAT_CONFIG_H

#include "compat.h"

void klyron_compat_config_set_version(klyron_compat_config_t* config, const char* version);

#endif /* KLYRON_COMPAT_CONFIG_H */
