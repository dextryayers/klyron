#ifndef KLYRON_KLYRON_DNS_TYPES_HPP
#define KLYRON_KLYRON_DNS_TYPES_HPP

#include <string>
#include <vector>
#include <map>

namespace klyron {
struct DnsResolver {};
struct DnsRecord {};
struct DnsResult {};
}

#endif