// Type definitions for klyron_fs

export interface FileSystem {
  // FileSystem members
}

export interface FileInfo {
  // FileInfo members
}

export interface FsEvent {
  // FsEvent members
}

export interface FileWatcher {
  // FileWatcher members
}

export interface FileWatcherBuilder {
  // FileWatcherBuilder members
}