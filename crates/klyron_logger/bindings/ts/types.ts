// Type definitions for klyron_logger

export interface Logger {
  // Logger members
}

export interface LoggerConfig {
  // LoggerConfig members
}

export interface LogLevel {
  // LogLevel members
}

export interface LogEntry {
  // LogEntry members
}

export interface LogFile {
  // LogFile members
}