#ifndef KLYRON_KLYRON_LOGGER_TYPES_HPP
#define KLYRON_KLYRON_LOGGER_TYPES_HPP

#include <string>
#include <vector>
#include <map>

namespace klyron {
struct Logger {};
struct LoggerConfig {};
struct LogLevel {};
struct LogEntry {};
struct LogFile {};
}

#endif