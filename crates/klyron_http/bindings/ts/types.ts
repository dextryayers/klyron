// Type definitions for klyron_http

export interface HttpServer {
  // HttpServer members
}

export interface ServerConfig {
  // ServerConfig members
}

export interface TlsConfig {
  // TlsConfig members
}

export interface RouteConfig {
  // RouteConfig members
}

export interface HttpScheme {
  // HttpScheme members
}