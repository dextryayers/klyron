#ifndef KLYRON_KLYRON_HTTP_TYPES_HPP
#define KLYRON_KLYRON_HTTP_TYPES_HPP

#include <string>
#include <vector>
#include <map>

namespace klyron {
struct HttpServer {};
struct ServerConfig {};
struct TlsConfig {};
struct RouteConfig {};
struct HttpScheme {};
}

#endif