#include "workspace_api.h"
#include <stdio.h>

int klyron_workspace_execute(void) {
    // TODO: implement
    return 0;
}

void klyron_workspace_serve(const char* addr) {
    printf("[workspace] serving on %s\n", addr);
}
