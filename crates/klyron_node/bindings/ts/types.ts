// Type definitions for klyron_node

export interface NodeGlobals {
  // NodeGlobals members
}

export interface NodeModuleRef {
  // NodeModuleRef members
}

export interface RequireFunction {
  // RequireFunction members
}

export interface RequireContext {
  // RequireContext members
}