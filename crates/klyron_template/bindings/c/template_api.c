#include "template_api.h"
#include <stdio.h>

int klyron_template_execute(void) {
    // TODO: implement
    return 0;
}

void klyron_template_serve(const char* addr) {
    printf("[template] serving on %s\n", addr);
}
