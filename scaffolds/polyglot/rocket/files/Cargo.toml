[package]
name = "{{ name }}"
version = "{{ version }}"
description = "{{ description }}"
edition = "2021"

[dependencies]
rocket = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
diesel = { version = "2", features = ["postgres"] }
diesel_migrations = "2"
dotenvy = "0.15"
