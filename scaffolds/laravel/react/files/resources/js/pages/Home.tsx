import { useState } from 'react'; export default function Home() { const [count, setCount] = useState(0); return (<div className="flex min-h-screen flex-col items-center justify-center bg-gray-100"><h1 className="text-4xl font-bold sm:text-6xl">{{ name }}</h1><p className="mt-4 text-lg text-gray-600">{{ description }}</p><button className="mt-6 rounded-md bg-indigo-600 px-4 py-2 text-white" onClick={() => setCount(c => c+1)}>count is {count}</button></div>); }
