[variables]
name = { type = "string", prompt = "Project name", default = "my-astro-site" }
description = { type = "string", prompt = "Project description", default = "An Astro site" }
author = { type = "string", prompt = "Author name", default = "Klyron" }
version = { type = "string", prompt = "Initial version", default = "0.1.0" }
license = { type = "string", prompt = "License", default = "MIT" }
